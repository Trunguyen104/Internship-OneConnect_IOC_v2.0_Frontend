const ACCESS_TOKEN_KEY = 'accessToken';

export function setAccessToken(token) {
  // Handled via HttpOnly cookies
}

export function getAccessToken() {
  return null;
}

export function clearAuth() {
  // Managed via server-side cookies
}
